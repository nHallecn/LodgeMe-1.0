/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      // Add any external image domains here if you plan to use them
      // e.g., 'res.cloudinary.com', 'example.com'
    ],
  },
};

module.exports = nextConfig;